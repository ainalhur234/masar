<!doctype html>
<html lang="ar" dir="rtl">
 <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>منصة مسار للرياضيات</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Kufi+Arabic:wght@100;200;300;400;500;600;700;800;900&amp;family=Cairo:wght@200;300;400;500;600;700;800;900&amp;family=Tajawal:wght@200;300;400;500;700;800;900&amp;display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Granada:wght@400;700;900&amp;display=swap" rel="stylesheet">
  <script src="/_sdk/element_sdk.js"></script>
  <style>
        body {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 25%, #90caf9 50%, #64b5f6 75%, #42a5f5 100%);
            min-height: 100%;
            height: 100%;
        }

        html {
            height: 100%;
        }

        * {
            box-sizing: border-box;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 25px;
            background: url('https://c.top4top.io/p_3568j8vko1.jpg');
            background-size: cover;
            background-position: center center;
            background-repeat: no-repeat;
            background-attachment: local;
            padding: 30px 35px;
            border-radius: 20px;
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.2);
            border: 3px solid rgba(255, 255, 255, 0.4);
            position: relative;
            overflow: hidden;
            min-height: 250px;
            opacity: 0.85;
        }

        .header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(2px);
            z-index: 1;
        }

        .header > * {
            position: relative;
            z-index: 2;
        }

        .year-badge {
            color: white;
            padding: 12px 24px;
            border-radius: 25px;
            font-size: 26px;
            font-weight: 900;
            font-family: 'Arial Black', 'Helvetica', 'Impact', sans-serif;
            z-index: 3;
            text-align: center;
            margin: 15px auto;
            display: inline-block;
            position: relative;
            letter-spacing: 3px;
            text-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
            animation: yearGlow 3s ease-in-out infinite alternate;
        }

        @keyframes yearGlow {
            0% {
                box-shadow: 0 6px 20px rgba(99, 102, 241, 0.4), 0 0 30px rgba(139, 92, 246, 0.3);
                transform: scale(1);
            }
            100% {
                box-shadow: 0 8px 25px rgba(99, 102, 241, 0.6), 0 0 40px rgba(139, 92, 246, 0.5);
                transform: scale(1.02);
            }
        }

        .math-symbols {
            position: absolute;
            top: 15px;
            left: 20px;
            font-size: 28px;
            z-index: 3;
            animation: float 3s ease-in-out infinite;
            color: rgba(255, 255, 255, 0.8);
            text-shadow: 0 0 15px rgba(255, 215, 0, 0.6);
        }



        .title {
            font-size: 48px;
            font-weight: 900;
            color: #ffffff;
            margin: 0 0 12px 0;
            letter-spacing: 2px;
            text-shadow: 
                2px 2px 0px #0d47a1,
                4px 4px 0px #01579b,
                6px 6px 0px #003c8f,
                8px 8px 15px rgba(0, 0, 0, 0.8);
            font-family: 'Granada', 'Arial Black', 'Helvetica', sans-serif !important;
            text-align: center;
            display: inline-block;
            position: relative;
            padding: 25px 35px;
            text-rendering: optimizeLegibility;
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        .math-symbol {
            position: absolute;
            font-size: 32px;
            color: rgba(255, 255, 255, 0.95);
            animation: floatMath 5s ease-in-out infinite;
            text-shadow: 
                0 0 15px rgba(255, 215, 0, 0.9),
                0 0 25px rgba(255, 215, 0, 0.6),
                2px 2px 4px rgba(0, 0, 0, 0.3);
            font-weight: bold;
            z-index: 4;
        }

        .math-symbol:nth-child(1) {
            top: -18px;
            left: -25px;
            animation-delay: 0s;
            font-size: 36px;
        }

        .math-symbol:nth-child(2) {
            top: -15px;
            right: -28px;
            animation-delay: 1.2s;
            font-size: 34px;
        }

        .math-symbol:nth-child(3) {
            bottom: -18px;
            left: -20px;
            animation-delay: 2.4s;
            font-size: 38px;
        }

        .math-symbol:nth-child(4) {
            bottom: -15px;
            right: -25px;
            animation-delay: 3.6s;
            font-size: 35px;
        }

        .math-symbol:nth-child(5) {
            top: 50%;
            left: -30px;
            transform: translateY(-50%);
            animation-delay: 0.8s;
            font-size: 40px;
        }

        .math-symbol:nth-child(6) {
            top: 50%;
            right: -30px;
            transform: translateY(-50%);
            animation-delay: 2s;
            font-size: 37px;
        }

        .math-symbol:nth-child(7) {
            top: 25%;
            left: -35px;
            animation-delay: 4.2s;
            font-size: 33px;
        }

        .math-symbol:nth-child(8) {
            top: 75%;
            right: -35px;
            animation-delay: 1.8s;
            font-size: 39px;
        }

        @keyframes floatMath {
            0%, 100% {
                transform: translateY(0px) rotate(0deg) scale(1);
                opacity: 0.85;
                text-shadow: 
                    0 0 15px rgba(255, 215, 0, 0.9),
                    0 0 25px rgba(255, 215, 0, 0.6),
                    2px 2px 4px rgba(0, 0, 0, 0.3);
            }
            20% {
                transform: translateY(-10px) rotate(8deg) scale(1.15);
                opacity: 1;
                text-shadow: 
                    0 0 20px rgba(255, 215, 0, 1),
                    0 0 35px rgba(255, 215, 0, 0.8),
                    3px 3px 6px rgba(0, 0, 0, 0.4);
            }
            40% {
                transform: translateY(-18px) rotate(-5deg) scale(1.08);
                opacity: 0.95;
                text-shadow: 
                    0 0 18px rgba(255, 215, 0, 0.95),
                    0 0 30px rgba(255, 215, 0, 0.7),
                    2px 2px 5px rgba(0, 0, 0, 0.35);
            }
            60% {
                transform: translateY(-12px) rotate(3deg) scale(1.12);
                opacity: 1;
                text-shadow: 
                    0 0 22px rgba(255, 215, 0, 1),
                    0 0 40px rgba(255, 215, 0, 0.75),
                    4px 4px 7px rgba(0, 0, 0, 0.4);
            }
            80% {
                transform: translateY(-6px) rotate(-2deg) scale(1.05);
                opacity: 0.9;
                text-shadow: 
                    0 0 16px rgba(255, 215, 0, 0.9),
                    0 0 28px rgba(255, 215, 0, 0.65),
                    2px 2px 4px rgba(0, 0, 0, 0.3);
            }
        }

        @keyframes titleShine {
            0% { 
                background-position: 0% 50%;
                text-shadow: 
                    0 0 15px rgba(255, 255, 255, 0.9),
                    0 0 25px rgba(255, 255, 255, 0.7),
                    0 0 35px rgba(255, 255, 255, 0.5),
                    3px 3px 0px #1565c0,
                    6px 6px 0px #0d47a1,
                    9px 9px 0px #01579b,
                    12px 12px 20px rgba(0, 0, 0, 0.6);
            }
            25% { 
                background-position: 25% 50%;
                text-shadow: 
                    0 0 20px rgba(255, 255, 255, 1),
                    0 0 30px rgba(255, 255, 255, 0.8),
                    0 0 40px rgba(255, 255, 255, 0.6),
                    3px 3px 0px #1976d2,
                    6px 6px 0px #1565c0,
                    9px 9px 0px #0d47a1,
                    12px 12px 25px rgba(0, 0, 0, 0.7);
            }
            50% { 
                background-position: 50% 50%;
                text-shadow: 
                    0 0 25px rgba(255, 255, 255, 1),
                    0 0 35px rgba(255, 255, 255, 0.9),
                    0 0 45px rgba(255, 255, 255, 0.7),
                    3px 3px 0px #2196f3,
                    6px 6px 0px #1976d2,
                    9px 9px 0px #1565c0,
                    12px 12px 30px rgba(0, 0, 0, 0.8);
            }
            75% { 
                background-position: 75% 50%;
                text-shadow: 
                    0 0 20px rgba(255, 255, 255, 1),
                    0 0 30px rgba(255, 255, 255, 0.8),
                    0 0 40px rgba(255, 255, 255, 0.6),
                    3px 3px 0px #1976d2,
                    6px 6px 0px #1565c0,
                    9px 9px 0px #0d47a1,
                    12px 12px 25px rgba(0, 0, 0, 0.7);
            }
            100% { 
                background-position: 100% 50%;
                text-shadow: 
                    0 0 15px rgba(255, 255, 255, 0.9),
                    0 0 25px rgba(255, 255, 255, 0.7),
                    0 0 35px rgba(255, 255, 255, 0.5),
                    3px 3px 0px #1565c0,
                    6px 6px 0px #0d47a1,
                    9px 9px 0px #01579b,
                    12px 12px 20px rgba(0, 0, 0, 0.6);
            }
        }

        @keyframes titleFloat {
            0% { 
                transform: translateY(0px) scale(1) rotateX(0deg);
            }
            50% { 
                transform: translateY(-2px) scale(1.005) rotateX(1deg);
            }
            100% { 
                transform: translateY(-4px) scale(1.01) rotateX(0deg);
            }
        }

        .subtitle {
            font-size: 16px;
            color: #ffffff;
            margin: 0 0 15px 0;
            font-weight: 600;
            text-shadow: 2px 2px 6px rgba(0, 0, 0, 0.7), 0 0 15px rgba(0, 0, 0, 0.5);
        }

        .teacher-info {
            font-size: 13px;
            color: #ffffff;
            margin: 0;
            padding-top: 15px;
            border-top: 2px solid rgba(255, 255, 255, 0.8);
            font-weight: 500;
            text-shadow: 2px 2px 6px rgba(0, 0, 0, 0.7), 0 0 15px rgba(0, 0, 0, 0.5);
        }

        .navigation-bar {
            background: linear-gradient(135deg, rgba(144, 202, 249, 0.9), rgba(100, 181, 246, 0.8));
            padding: 15px 0;
            border-radius: 16px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            margin-bottom: 30px;
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.4);
            position: relative;
            z-index: 1000;
        }

        .nav-container {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 20px;
            flex-wrap: wrap;
        }

        .dropdown {
            position: relative;
            display: inline-block;
        }

        .dropdown-button {
            background: linear-gradient(135deg, #64b5f6, #42a5f5);
            color: white;
            padding: 12px 20px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 12px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
            box-shadow: 0 2px 10px rgba(100, 181, 246, 0.3), inset 0 1px 0 rgba(255, 255, 255, 0.2);
            min-width: 160px;
            justify-content: space-between;
            letter-spacing: -0.2px;
            position: relative;
            overflow: hidden;
        }

        .dropdown-button:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 15px rgba(100, 181, 246, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.3);
            background: linear-gradient(135deg, #42a5f5, #2196f3);
            border-color: rgba(255, 255, 255, 0.5);
        }

        .dropdown-icon {
            font-size: 16px;
        }

        .dropdown-arrow {
            transition: transform 0.3s ease;
            font-size: 12px;
        }

        .dropdown.active .dropdown-arrow {
            transform: rotate(180deg);
        }

        .dropdown-content {
            position: absolute;
            top: 100%;
            left: 0;
            right: 0;
            background: white;
            border-radius: 12px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
            opacity: 0;
            visibility: hidden;
            transform: translateY(-8px);
            transition: all 0.3s ease;
            z-index: 9999;
            margin-top: 6px;
            border: 1px solid rgba(99, 102, 241, 0.1);
        }

        .dropdown.active .dropdown-content {
            opacity: 1;
            visibility: visible;
            transform: translateY(0);
        }

        .dropdown-item {
            padding: 12px 16px;
            cursor: pointer;
            transition: all 0.2s ease;
            border-bottom: 1px solid #f1f5f9;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 14px;
            color: #334155;
            font-weight: 500;
        }

        .dropdown-item:last-child {
            border-bottom: none;
            border-radius: 0 0 12px 12px;
        }

        .dropdown-item:first-child {
            border-radius: 12px 12px 0 0;
        }

        .dropdown-item:hover {
            background: linear-gradient(135deg, #6366f1, #8b5cf6);
            color: white;
            transform: translateX(3px);
        }

        .dropdown-item-icon {
            font-size: 16px;
            width: 18px;
            text-align: center;
        }

        .grade-item {
            position: relative;
            justify-content: space-between;
        }

        .grade-arrow {
            font-size: 12px;
            transition: transform 0.3s ease;
            color: #64748b;
        }

        .grade-item:hover .grade-arrow {
            transform: translateX(3px);
            color: white;
        }

        .grade-submenu {
            display: none;
            background: #f8fafc;
            border-radius: 8px;
            margin: 5px 0;
            border: 1px solid #e2e8f0;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            z-index: 10000;
            position: relative;
        }

        .grade-submenu.active {
            display: block;
            animation: slideDown 0.3s ease;
        }

        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .submenu-item {
            padding: 10px 20px;
            cursor: pointer;
            transition: all 0.2s ease;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 13px;
            color: #475569;
            font-weight: 500;
            border-bottom: 1px solid #e2e8f0;
        }

        .submenu-item:last-child {
            border-bottom: none;
            border-radius: 0 0 8px 8px;
        }

        .submenu-item:first-child {
            border-radius: 8px 8px 0 0;
        }

        .submenu-item:hover {
            background: linear-gradient(135deg, #3b82f6, #2563eb);
            color: white;
            transform: translateX(5px);
        }

        .submenu-icon {
            font-size: 14px;
            width: 16px;
            text-align: center;
        }

        .features-section {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .feature-card {
            background: linear-gradient(135deg, rgba(144, 202, 249, 0.9), rgba(100, 181, 246, 0.8));
            padding: 25px 20px;
            border-radius: 16px;
            text-align: center;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.4);
            backdrop-filter: blur(15px);
        }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
            border-color: #6366f1;
        }

        .feature-icon {
            font-size: 40px;
            margin-bottom: 15px;
            display: block;
        }

        .feature-title {
            font-size: 18px;
            font-weight: 700;
            color: #ffffff;
            margin: 0 0 10px 0;
            letter-spacing: -0.3px;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);
        }

        .feature-description {
            font-size: 14px;
            color: rgba(255, 255, 255, 0.9);
            line-height: 1.5;
            margin: 0;
            text-shadow: 1px 1px 1px rgba(0, 0, 0, 0.2);
        }

        .whatsapp-section {
            text-align: center;
            margin-top: 30px;
            padding: 8px;
            background: linear-gradient(135deg, rgba(129, 199, 132, 0.05), rgba(102, 187, 106, 0.04));
            border-radius: 8px;
            border: 1px solid rgba(129, 199, 132, 0.1);
        }

        .whatsapp-button {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: linear-gradient(135deg, #81c784, #66bb6a);
            color: white;
            padding: 10px 20px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 50px;
            text-decoration: none;
            font-size: 14px;
            font-weight: 600;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(129, 199, 132, 0.3), inset 0 1px 0 rgba(255, 255, 255, 0.2);
            letter-spacing: -0.2px;
            position: relative;
            overflow: hidden;
        }

        .whatsapp-button:hover {
            background: linear-gradient(135deg, #66bb6a, #4caf50);
            transform: scale(1.03);
            box-shadow: 0 6px 20px rgba(129, 199, 132, 0.4), inset 0 1px 0 rgba(255, 255, 255, 0.3);
            border-color: rgba(255, 255, 255, 0.5);
        }

        .whatsapp-icon {
            font-size: 16px;
        }

        .description-section {
            margin: 30px 0;
        }

        .description-card {
            background: linear-gradient(135deg, rgba(187, 222, 251, 0.9), rgba(144, 202, 249, 0.8));
            padding: 30px;
            border-radius: 20px;
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.1);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.4);
        }

        .description-title {
            font-size: 28px;
            font-weight: 800;
            color: #ffffff;
            text-align: center;
            margin: 0 0 25px 0;
            font-family: 'Noto Kufi Arabic', 'Cairo', 'Tajawal', sans-serif;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .description-text {
            font-size: 16px;
            line-height: 1.8;
            color: #ffffff;
            text-align: center;
            margin: 0 0 30px 0;
            font-weight: 500;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.2);
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin: 30px 0;
        }

        .feature-item {
            text-align: center;
            padding: 20px;
            background: linear-gradient(135deg, #f8fafc, #f1f5f9);
            border-radius: 16px;
            border: 1px solid rgba(99, 102, 241, 0.1);
            transition: all 0.3s ease;
        }

        .feature-item:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(99, 102, 241, 0.15);
            border-color: #6366f1;
        }

        .feature-emoji {
            font-size: 32px;
            display: block;
            margin-bottom: 12px;
        }

        .feature-name {
            font-size: 16px;
            font-weight: 700;
            color: #1e293b;
            margin: 0 0 8px 0;
        }

        .feature-desc {
            font-size: 13px;
            color: #64748b;
            margin: 0;
            line-height: 1.5;
        }

        .mission-vision-section {
            margin: 30px 0;
        }

        .mission-vision {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
        }

        .mission-item, .vision-item {
            padding: 25px;
            background: linear-gradient(135deg, #90caf9, #64b5f6);
            border-radius: 16px;
            color: white;
            text-align: center;
            box-shadow: 0 6px 20px rgba(144, 202, 249, 0.3);
        }

        .mission-title, .vision-title {
            font-size: 18px;
            font-weight: 700;
            margin: 0 0 15px 0;
            font-family: 'Noto Kufi Arabic', 'Cairo', 'Tajawal', sans-serif;
        }

        .mission-text, .vision-text {
            font-size: 14px;
            line-height: 1.6;
            margin: 0;
            opacity: 0.95;
        }

        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }

            .header {
                padding: 15px 20px;
                margin-bottom: 20px;
            }

            .title {
                font-size: 24px;
            }

            .subtitle {
                font-size: 13px;
            }

            .teacher-info {
                font-size: 11px;
            }

            .nav-container {
                flex-direction: column;
                gap: 12px;
            }

            .dropdown-button {
                min-width: 250px;
                font-size: 13px;
                padding: 10px 16px;
            }

            .features-section {
                grid-template-columns: 1fr;
                gap: 15px;
                margin: 20px 0;
            }

            .feature-card {
                padding: 20px 15px;
            }

            .feature-icon {
                font-size: 32px;
            }

            .feature-title {
                font-size: 16px;
            }

            .feature-description {
                font-size: 13px;
            }

            .description-card {
                padding: 20px;
            }

            .description-title {
                font-size: 22px;
            }

            .description-text {
                font-size: 14px;
            }

            .features-grid {
                grid-template-columns: 1fr;
                gap: 15px;
            }

            .mission-vision {
                grid-template-columns: 1fr;
                gap: 15px;
            }

            .mission-item, .vision-item {
                padding: 20px;
            }

            .whatsapp-button {
                font-size: 14px;
                padding: 12px 24px;
            }
        }

        /* تصميم نافذة الفيديو */
        .video-modal {
            display: none;
            position: fixed;
            z-index: 10000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(5px);
        }

        .video-modal.active {
            display: flex;
            align-items: center;
            justify-content: center;
            animation: fadeIn 0.3s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .video-modal-content {
            background: white;
            border-radius: 16px;
            width: 90%;
            max-width: 900px;
            max-height: 90%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            animation: slideUp 0.3s ease;
        }

        @keyframes slideUp {
            from { 
                transform: translateY(50px);
                opacity: 0;
            }
            to { 
                transform: translateY(0);
                opacity: 1;
            }
        }

        .video-modal-header {
            background: linear-gradient(135deg, #64b5f6, #42a5f5);
            color: white;
            padding: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .video-modal-header h3 {
            margin: 0;
            font-size: 20px;
            font-weight: 700;
            font-family: 'Noto Kufi Arabic', 'Cairo', 'Tajawal', sans-serif;
        }

        .close-video {
            background: none;
            border: none;
            color: white;
            font-size: 28px;
            cursor: pointer;
            padding: 0;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .close-video:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: scale(1.1);
        }

        .video-container {
            position: relative;
            width: 100%;
            height: 0;
            padding-bottom: 56.25%; /* نسبة 16:9 */
            background: #000;
        }

        .video-container iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        @media (max-width: 768px) {
            .video-modal-content {
                width: 95%;
                max-height: 85%;
            }

            .video-modal-header {
                padding: 15px;
            }

            .video-modal-header h3 {
                font-size: 18px;
            }

            .close-video {
                font-size: 24px;
                width: 35px;
                height: 35px;
            }
        }

        @media (max-width: 480px) {
            .container {
                padding: 12px;
            }

            .header {
                padding: 12px 15px;
            }

            .title {
                font-size: 20px;
            }

            .subtitle {
                font-size: 12px;
            }

            .dropdown-button {
                min-width: 100%;
                font-size: 12px;
            }

            .video-modal-content {
                width: 98%;
                max-height: 80%;
            }

            .video-modal-header {
                padding: 12px;
            }

            .video-modal-header h3 {
                font-size: 16px;
            }
        }
    </style>
  <style>@view-transition { navigation: auto; }</style>
  <script src="/_sdk/data_sdk.js" type="text/javascript"></script>
  <script src="https://cdn.tailwindcss.com" type="text/javascript"></script>
 </head>
 <body>
  <div class="container">
   <div class="header">
    <h1 class="title" id="platform-title"><span class="math-symbol">∑</span> <span class="math-symbol">∫</span> <span class="math-symbol">π</span> <span class="math-symbol">√</span> <span class="math-symbol">∞</span> <span class="math-symbol">α</span> <span class="math-symbol">θ</span> <span class="math-symbol">Ω</span> منصة مسار</h1>
    <p class="subtitle" id="platform-subtitle">بوابتك إلى عالم الرياضيات</p>
    <div class="year-badge">
     2025
    </div>
    <p class="teacher-info" id="teacher-info">إعداد: أستاذ محمد المزروعي - معلم أول مادة الرياضيات</p>
   </div>
   <div class="description-section">
    <div class="description-card">
     <h2 class="description-title" id="description-title">من نحن</h2>
     <p class="description-text" id="description-text">منصة مسار منصة تعليمية متخصصة في مادة الرياضيات، تهدف إلى تقديم محتوى تعليمي متميز. نقدم تمارين إضافية مع حلول مفصلة عبر مقاطع فيديو لتسهيل فهم الدروس وتحسين الأداء الدراسي.</p>
    </div>
   </div>
   <nav class="navigation-bar">
    <div class="nav-container">
     <div class="dropdown"><button class="dropdown-button"> <span class="dropdown-icon">📚</span> <span id="menu-library">مكتبة الرياضيات</span> <span class="dropdown-arrow">▼</span> </button>
      <div class="dropdown-content">
       <div class="dropdown-item grade-item" data-grade="5"><span class="dropdown-item-icon">📚</span> <span>الصف الخامس</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-5-submenu">
        <div class="submenu-item"><span class="submenu-icon">📖</span> <span>كتاب الطالب - الصف الخامس</span>
        </div>
        <div class="submenu-item"><span class="submenu-icon">📝</span> <span>كتاب النشاط - الصف الخامس</span>
        </div>
       </div>
       <div class="dropdown-item grade-item" data-grade="6"><span class="dropdown-item-icon">📚</span> <span>الصف السادس</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-6-submenu">
        <div class="submenu-item"><span class="submenu-icon">📖</span> <span>كتاب الطالب - الصف السادس</span>
        </div>
        <div class="submenu-item"><span class="submenu-icon">📝</span> <span>كتاب النشاط - الصف السادس</span>
        </div>
       </div>
       <div class="dropdown-item grade-item" data-grade="7"><span class="dropdown-item-icon">📚</span> <span>الصف السابع</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-7-submenu">
        <div class="submenu-item"><span class="submenu-icon">📖</span> <span>كتاب الطالب - الصف السابع</span>
        </div>
        <div class="submenu-item"><span class="submenu-icon">📝</span> <span>كتاب النشاط - الصف السابع</span>
        </div>
       </div>
       <div class="dropdown-item grade-item" data-grade="8"><span class="dropdown-item-icon">📚</span> <span>الصف الثامن</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-8-submenu">
        <div class="submenu-item"><span class="submenu-icon">📖</span> <span>كتاب الطالب - الصف الثامن</span>
        </div>
        <div class="submenu-item"><span class="submenu-icon">📝</span> <span>كتاب النشاط - الصف الثامن</span>
        </div>
       </div>
       <div class="dropdown-item grade-item" data-grade="9"><span class="dropdown-item-icon">📚</span> <span>الصف التاسع</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-9-submenu">
        <div class="submenu-item"><span class="submenu-icon">📖</span> <span>كتاب الطالب - الصف التاسع</span>
        </div>
        <div class="submenu-item"><span class="submenu-icon">📝</span> <span>كتاب النشاط - الصف التاسع</span>
        </div>
       </div>
       <div class="dropdown-item grade-item" data-grade="10"><span class="dropdown-item-icon">📚</span> <span>الصف العاشر</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-10-submenu">
        <div class="submenu-item"><span class="submenu-icon">📖</span> <span>كتاب الطالب - الصف العاشر</span>
        </div>
        <div class="submenu-item"><span class="submenu-icon">📝</span> <span>كتاب النشاط - الصف العاشر</span>
        </div>
       </div>
       <div class="dropdown-item grade-item" data-grade="11-basic"><span class="dropdown-item-icon">📚</span> <span>الصف الحادي عشر أساسي</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-11-basic-submenu">
        <div class="submenu-item"><span class="submenu-icon">📖</span> <span>كتاب الطالب - الحادي عشر أساسي</span>
        </div>
        <div class="submenu-item"><span class="submenu-icon">📝</span> <span>كتاب النشاط - الحادي عشر أساسي</span>
        </div>
       </div>
       <div class="dropdown-item grade-item" data-grade="11-advanced"><span class="dropdown-item-icon">📚</span> <span>الصف الحادي عشر متقدم</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-11-advanced-submenu">
        <div class="submenu-item"><span class="submenu-icon">📖</span> <span>كتاب الطالب - الحادي عشر متقدم</span>
        </div>
        <div class="submenu-item"><span class="submenu-icon">📝</span> <span>كتاب النشاط - الحادي عشر متقدم</span>
        </div>
       </div>
       <div class="dropdown-item grade-item" data-grade="12-basic"><span class="dropdown-item-icon">📚</span> <span>الصف الثاني عشر أساسي</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-12-basic-submenu">
        <div class="submenu-item"><span class="submenu-icon">📖</span> <span>كتاب الطالب - الثاني عشر أساسي</span>
        </div>
        <div class="submenu-item"><span class="submenu-icon">📝</span> <span>كتاب النشاط - الثاني عشر أساسي</span>
        </div>
       </div>
       <div class="dropdown-item grade-item" data-grade="12-advanced"><span class="dropdown-item-icon">📚</span> <span>الصف الثاني عشر متقدم</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-12-advanced-submenu">
        <div class="submenu-item"><span class="submenu-icon">📖</span> <span>كتاب الطالب - الثاني عشر متقدم</span>
        </div>
        <div class="submenu-item"><span class="submenu-icon">📝</span> <span>كتاب النشاط - الثاني عشر متقدم</span>
        </div>
       </div>
      </div>
     </div>
     <div class="dropdown"><button class="dropdown-button"> <span class="dropdown-icon">🎥</span> <span id="menu-videos">فيديوهات تمارين محلولة</span> <span class="dropdown-arrow">▼</span> </button>
      <div class="dropdown-content">
       <div class="dropdown-item grade-item" data-grade="10-videos"><span class="dropdown-item-icon">📚</span> <span>الصف العاشر</span> <span class="grade-arrow">◀</span>
       </div>
       <div class="grade-submenu" id="grade-10-videos-submenu">
        <div class="submenu-item" id="lesson-1-video"><span class="submenu-icon">🎥</span> <span>الدرس الأول</span>
        </div>
        <div class="submenu-item" id="lesson-2-video"><span class="submenu-icon">🎥</span> <span>الدرس الثاني</span>
        </div>
       </div>
      </div>
     </div>
     <div class="dropdown"><button class="dropdown-button"> <span class="dropdown-icon">✍️</span> <span id="menu-exercises">تمارين ومسائل واختبارات</span> <span class="dropdown-arrow">▼</span> </button>
      <div class="dropdown-content">
       <div class="dropdown-item"><span class="dropdown-item-icon">📋</span> <span>تمارين الكتاب</span>
       </div>
       <div class="dropdown-item"><span class="dropdown-item-icon">🎯</span> <span>تمارين إضافية</span>
       </div>
       <div class="dropdown-item"><span class="dropdown-item-icon">📄</span> <span>اختبارات سابقة</span>
       </div>
       <div class="dropdown-item"><span class="dropdown-item-icon">🏅</span> <span>اختبارات تجريبية</span>
       </div>
      </div>
     </div>
    </div>
   </nav>
   <div class="mission-vision-section">
    <div class="mission-vision">
     <div class="mission-item">
      <h3 class="mission-title" id="mission-title">رسالتنا</h3>
      <p class="mission-text" id="mission-text">تمكين الطلاب من إتقان الرياضيات عبر تقديم محتوى تعليمي متميز يواكب أحدث الطرق التعليمية، ويساهم في تطوير مهارات التفكير المنطقي.</p>
     </div>
     <div class="vision-item">
      <h3 class="vision-title" id="vision-title">رؤيتنا</h3>
      <p class="vision-text" id="vision-text">أن نكون المنصة الرائدة في تعليم الرياضيات في المنطقة، ونساهم في إعداد جيل متميز في العلوم الرياضية.</p>
     </div>
    </div>
   </div><!-- نافذة عرض الفيديو -->
   <div id="video-modal" class="video-modal">
    <div class="video-modal-content">
     <div class="video-modal-header">
      <h3 id="video-title">الدرس الأول</h3><button class="close-video" id="close-video">×</button>
     </div>
     <div class="video-container"><iframe id="video-frame" src="" frameborder="0" allowfullscreen></iframe>
     </div>
    </div>
   </div>
   <div class="whatsapp-section"><a href="https://wa.me/96586733955" target="_blank" rel="noopener noreferrer" class="whatsapp-button"> <span class="whatsapp-icon">📱</span> <span id="whatsapp-label">تواصل معنا عبر الواتساب</span> </a>
   </div>
  </div>
  <script>
        const defaultConfig = {
            platform_title: "منصة مسار",
            platform_subtitle: "بوابتك إلى عالم الرياضيات",
            teacher_info: "إعداد: أستاذ محمد المزروعي - معلم أول مادة الرياضيات",
            menu_about: "من نحن",
            menu_library: "مكتبة الرياضيات",
            menu_videos: "فيديوهات تمارين محلولة",
            menu_exercises: "تمارين ومسائل واختبارات سابقة",
            whatsapp_label: "تواصل معنا عبر الواتساب",
            description_title: "من نحن",
            description_text: "منصة مسار منصة تعليمية متخصصة في مادة الرياضيات، تهدف إلى تقديم محتوى تعليمي متميز. نقدم تمارين إضافية مع حلول مفصلة عبر مقاطع فيديو لتسهيل فهم الدروس وتحسين الأداء الدراسي.",
            feature_comprehensive: "شروحات شاملة",
            feature_comprehensive_desc: "دروس مفصلة تغطي جميع المناهج الدراسية",
            feature_videos: "فيديوهات تعليمية",
            feature_videos_desc: "شروحات مرئية واضحة ومبسطة",
            feature_exercises: "تمارين متنوعة",
            feature_exercises_desc: "مجموعة واسعة من التمارين والاختبارات",
            feature_teacher: "معلم خبير",
            feature_teacher_desc: "إشراف أستاذ محمد المزروعي المتخصص",
            mission_title: "رسالتنا",
            mission_text: "تمكين الطلاب من إتقان الرياضيات عبر تقديم محتوى تعليمي متميز يواكب أحدث الطرق التعليمية، ويساهم في تطوير مهارات التفكير المنطقي.",
            vision_title: "رؤيتنا",
            vision_text: "أن نكون المنصة الرائدة في تعليم الرياضيات عربياً، وأن نساهم في بناء جيل متمكن من العلوم الرياضية قادر على الابتكار والإبداع.",
            background_color: "#e3f2fd",
            surface_color: "#90caf9",
            text_color: "#1565c0",
            primary_action: "#64b5f6",
            secondary_action: "#81c784"
        };

        async function onConfigChange(config) {
            // تحديث عناصر الرأس
            const titleElement = document.getElementById('platform-title');
            const subtitleElement = document.getElementById('platform-subtitle');
            const teacherElement = document.getElementById('teacher-info');
            const menuAboutElement = document.getElementById('menu-about');
            const menuLibraryElement = document.getElementById('menu-library');
            const menuVideosElement = document.getElementById('menu-videos');
            const menuExercisesElement = document.getElementById('menu-exercises');
            const whatsappLabelElement = document.getElementById('whatsapp-label');

            // تحديث عناصر قسم الوصف
            const descriptionTitleElement = document.getElementById('description-title');
            const descriptionTextElement = document.getElementById('description-text');
            const featureComprehensiveElement = document.getElementById('feature-comprehensive');
            const featureComprehensiveDescElement = document.getElementById('feature-comprehensive-desc');
            const featureVideosElement = document.getElementById('feature-videos');
            const featureVideosDescElement = document.getElementById('feature-videos-desc');
            const featureExercisesElement = document.getElementById('feature-exercises');
            const featureExercisesDescElement = document.getElementById('feature-exercises-desc');
            const featureTeacherElement = document.getElementById('feature-teacher');
            const featureTeacherDescElement = document.getElementById('feature-teacher-desc');
            const missionTitleElement = document.getElementById('mission-title');
            const missionTextElement = document.getElementById('mission-text');
            const visionTitleElement = document.getElementById('vision-title');
            const visionTextElement = document.getElementById('vision-text');

            // تحديث النصوص
            if (titleElement) titleElement.textContent = config.platform_title || defaultConfig.platform_title;
            if (subtitleElement) subtitleElement.textContent = config.platform_subtitle || defaultConfig.platform_subtitle;
            if (teacherElement) teacherElement.textContent = config.teacher_info || defaultConfig.teacher_info;
            if (menuAboutElement) menuAboutElement.textContent = config.menu_about || defaultConfig.menu_about;
            if (menuLibraryElement) menuLibraryElement.textContent = config.menu_library || defaultConfig.menu_library;
            if (menuVideosElement) menuVideosElement.textContent = config.menu_videos || defaultConfig.menu_videos;
            if (menuExercisesElement) menuExercisesElement.textContent = config.menu_exercises || defaultConfig.menu_exercises;
            if (whatsappLabelElement) whatsappLabelElement.textContent = config.whatsapp_label || defaultConfig.whatsapp_label;

            if (descriptionTitleElement) descriptionTitleElement.textContent = config.description_title || defaultConfig.description_title;
            if (descriptionTextElement) descriptionTextElement.textContent = config.description_text || defaultConfig.description_text;
            if (featureComprehensiveElement) featureComprehensiveElement.textContent = config.feature_comprehensive || defaultConfig.feature_comprehensive;
            if (featureComprehensiveDescElement) featureComprehensiveDescElement.textContent = config.feature_comprehensive_desc || defaultConfig.feature_comprehensive_desc;
            if (featureVideosElement) featureVideosElement.textContent = config.feature_videos || defaultConfig.feature_videos;
            if (featureVideosDescElement) featureVideosDescElement.textContent = config.feature_videos_desc || defaultConfig.feature_videos_desc;
            if (featureExercisesElement) featureExercisesElement.textContent = config.feature_exercises || defaultConfig.feature_exercises;
            if (featureExercisesDescElement) featureExercisesDescElement.textContent = config.feature_exercises_desc || defaultConfig.feature_exercises_desc;
            if (featureTeacherElement) featureTeacherElement.textContent = config.feature_teacher || defaultConfig.feature_teacher;
            if (featureTeacherDescElement) featureTeacherDescElement.textContent = config.feature_teacher_desc || defaultConfig.feature_teacher_desc;
            if (missionTitleElement) missionTitleElement.textContent = config.mission_title || defaultConfig.mission_title;
            if (missionTextElement) missionTextElement.textContent = config.mission_text || defaultConfig.mission_text;
            if (visionTitleElement) visionTitleElement.textContent = config.vision_title || defaultConfig.vision_title;
            if (visionTextElement) visionTextElement.textContent = config.vision_text || defaultConfig.vision_text;

            // تحديث الألوان
            const backgroundColor = config.background_color || defaultConfig.background_color;
            const surfaceColor = config.surface_color || defaultConfig.surface_color;
            const textColor = config.text_color || defaultConfig.text_color;
            const primaryAction = config.primary_action || defaultConfig.primary_action;
            const secondaryAction = config.secondary_action || defaultConfig.secondary_action;

            // تحديث خلفية الصفحة لتتناسق مع اللون الأساسي
            document.body.style.background = `linear-gradient(135deg, ${backgroundColor} 0%, #bbdefb 25%, ${surfaceColor} 50%, ${primaryAction} 75%, ${secondaryAction} 100%)`;
            
            // تحديث ألوان البطاقات والأسطح
            const featureCards = document.querySelectorAll('.feature-card');
            featureCards.forEach(card => {
                card.style.background = `linear-gradient(135deg, ${surfaceColor}e6, ${primaryAction}cc)`;
                card.style.backdropFilter = 'blur(15px)';
                card.style.border = '1px solid rgba(255, 255, 255, 0.4)';
            });
            
            const descriptionCard = document.querySelector('.description-card');
            if (descriptionCard) {
                descriptionCard.style.background = `linear-gradient(135deg, #bbdefbe6, ${surfaceColor}cc)`;
                descriptionCard.style.backdropFilter = 'blur(15px)';
                descriptionCard.style.border = '1px solid rgba(255, 255, 255, 0.4)';
            }

            const navigationBar = document.querySelector('.navigation-bar');
            if (navigationBar) {
                navigationBar.style.background = `linear-gradient(135deg, ${surfaceColor}e6, ${primaryAction}cc)`;
                navigationBar.style.backdropFilter = 'blur(15px)';
                navigationBar.style.border = '1px solid rgba(255, 255, 255, 0.4)';
            }
            
            // تحديث ألوان النصوص
            const featureTitles = document.querySelectorAll('.feature-title');
            featureTitles.forEach(title => {
                title.style.color = '#ffffff';
                title.style.textShadow = '1px 1px 2px rgba(0, 0, 0, 0.3)';
            });

            const descriptionTitleEl = document.querySelector('.description-title');
            if (descriptionTitleEl) {
                descriptionTitleEl.style.color = '#ffffff';
                descriptionTitleEl.style.textShadow = '2px 2px 4px rgba(0, 0, 0, 0.3)';
            }

            const descriptionTextEl = document.querySelector('.description-text');
            if (descriptionTextEl) {
                descriptionTextEl.style.color = '#ffffff';
                descriptionTextEl.style.textShadow = '1px 1px 2px rgba(0, 0, 0, 0.2)';
            }

            const featureDescriptions = document.querySelectorAll('.feature-description');
            featureDescriptions.forEach(desc => {
                desc.style.color = 'rgba(255, 255, 255, 0.9)';
                desc.style.textShadow = '1px 1px 1px rgba(0, 0, 0, 0.2)';
            });

            const featureNames = document.querySelectorAll('.feature-name');
            featureNames.forEach(name => {
                name.style.color = textColor;
            });

            // تحديث ألوان الأزرار والعناصر التفاعلية
            const dropdownButtons = document.querySelectorAll('.dropdown-button');
            dropdownButtons.forEach(button => {
                button.style.background = `linear-gradient(135deg, ${primaryAction}, #42a5f5)`;
                button.style.boxShadow = `0 2px 10px ${primaryAction}4d, inset 0 1px 0 rgba(255, 255, 255, 0.2)`;
                button.style.border = '2px solid rgba(255, 255, 255, 0.3)';
            });

            // تحديث ألوان الرسالة والرؤية
            const missionVisionItems = document.querySelectorAll('.mission-item, .vision-item');
            missionVisionItems.forEach(item => {
                item.style.background = `linear-gradient(135deg, ${surfaceColor}, ${primaryAction})`;
                item.style.boxShadow = `0 6px 20px ${surfaceColor}4d`;
            });
            
            const whatsappButton = document.querySelector('.whatsapp-button');
            if (whatsappButton) {
                whatsappButton.style.background = `linear-gradient(135deg, ${secondaryAction}, #66bb6a)`;
                whatsappButton.style.boxShadow = `0 4px 15px ${secondaryAction}4d, inset 0 1px 0 rgba(255, 255, 255, 0.2)`;
                whatsappButton.style.border = '2px solid rgba(255, 255, 255, 0.3)';
            }
        }

        function mapToCapabilities(config) {
            return {
                recolorables: [
                    {
                        get: () => config.background_color || defaultConfig.background_color,
                        set: (value) => {
                            if (window.elementSdk) {
                                window.elementSdk.config.background_color = value;
                                window.elementSdk.setConfig({ background_color: value });
                            }
                        }
                    },
                    {
                        get: () => config.surface_color || defaultConfig.surface_color,
                        set: (value) => {
                            if (window.elementSdk) {
                                window.elementSdk.config.surface_color = value;
                                window.elementSdk.setConfig({ surface_color: value });
                            }
                        }
                    },
                    {
                        get: () => config.text_color || defaultConfig.text_color,
                        set: (value) => {
                            if (window.elementSdk) {
                                window.elementSdk.config.text_color = value;
                                window.elementSdk.setConfig({ text_color: value });
                            }
                        }
                    },
                    {
                        get: () => config.primary_action || defaultConfig.primary_action,
                        set: (value) => {
                            if (window.elementSdk) {
                                window.elementSdk.config.primary_action = value;
                                window.elementSdk.setConfig({ primary_action: value });
                            }
                        }
                    },
                    {
                        get: () => config.secondary_action || defaultConfig.secondary_action,
                        set: (value) => {
                            if (window.elementSdk) {
                                window.elementSdk.config.secondary_action = value;
                                window.elementSdk.setConfig({ secondary_action: value });
                            }
                        }
                    }
                ],
                borderables: [],
                fontEditable: undefined,
                fontSizeable: undefined
            };
        }

        function mapToEditPanelValues(config) {
            return new Map([
                ["platform_title", config.platform_title || defaultConfig.platform_title],
                ["platform_subtitle", config.platform_subtitle || defaultConfig.platform_subtitle],
                ["teacher_info", config.teacher_info || defaultConfig.teacher_info],
                ["menu_about", config.menu_about || defaultConfig.menu_about],
                ["menu_library", config.menu_library || defaultConfig.menu_library],
                ["menu_videos", config.menu_videos || defaultConfig.menu_videos],
                ["menu_exercises", config.menu_exercises || defaultConfig.menu_exercises],
                ["whatsapp_label", config.whatsapp_label || defaultConfig.whatsapp_label],
                ["description_title", config.description_title || defaultConfig.description_title],
                ["description_text", config.description_text || defaultConfig.description_text],
                ["feature_comprehensive", config.feature_comprehensive || defaultConfig.feature_comprehensive],
                ["feature_comprehensive_desc", config.feature_comprehensive_desc || defaultConfig.feature_comprehensive_desc],
                ["feature_videos", config.feature_videos || defaultConfig.feature_videos],
                ["feature_videos_desc", config.feature_videos_desc || defaultConfig.feature_videos_desc],
                ["feature_exercises", config.feature_exercises || defaultConfig.feature_exercises],
                ["feature_exercises_desc", config.feature_exercises_desc || defaultConfig.feature_exercises_desc],
                ["feature_teacher", config.feature_teacher || defaultConfig.feature_teacher],
                ["feature_teacher_desc", config.feature_teacher_desc || defaultConfig.feature_teacher_desc],
                ["mission_title", config.mission_title || defaultConfig.mission_title],
                ["mission_text", config.mission_text || defaultConfig.mission_text],
                ["vision_title", config.vision_title || defaultConfig.vision_title],
                ["vision_text", config.vision_text || defaultConfig.vision_text]
            ]);
        }

        // إضافة وظائف القوائم المنسدلة
        document.addEventListener('DOMContentLoaded', function() {
            const dropdowns = document.querySelectorAll('.dropdown');
            
            dropdowns.forEach(dropdown => {
                const button = dropdown.querySelector('.dropdown-button');
                const content = dropdown.querySelector('.dropdown-content');
                
                button.addEventListener('click', function(e) {
                    e.stopPropagation();
                    
                    // إغلاق جميع القوائم الأخرى
                    dropdowns.forEach(otherDropdown => {
                        if (otherDropdown !== dropdown) {
                            otherDropdown.classList.remove('active');
                        }
                    });
                    
                    // تبديل حالة القائمة الحالية
                    dropdown.classList.toggle('active');
                });
                
                // إضافة تأثيرات للعناصر الفرعية
                const items = dropdown.querySelectorAll('.dropdown-item:not(.grade-item)');
                items.forEach(item => {
                    item.addEventListener('click', function() {
                        // يمكن إضافة وظائف هنا للتنقل أو العمليات الأخرى
                        dropdown.classList.remove('active');
                    });
                });

                // إضافة وظائف للصفوف مع القوائم الفرعية
                const gradeItems = dropdown.querySelectorAll('.grade-item');
                gradeItems.forEach(gradeItem => {
                    gradeItem.addEventListener('click', function(e) {
                        e.preventDefault();
                        e.stopPropagation();
                        
                        const gradeNumber = this.getAttribute('data-grade');
                        let submenu;
                        
                        // التعامل مع الصف العاشر للفيديوهات بشكل خاص
                        if (gradeNumber === '10-videos') {
                            submenu = document.getElementById('grade-10-videos-submenu');
                        } else if (gradeNumber === '11-basic') {
                            submenu = document.getElementById('grade-11-basic-submenu');
                        } else if (gradeNumber === '11-advanced') {
                            submenu = document.getElementById('grade-11-advanced-submenu');
                        } else if (gradeNumber === '12-basic') {
                            submenu = document.getElementById('grade-12-basic-submenu');
                        } else if (gradeNumber === '12-advanced') {
                            submenu = document.getElementById('grade-12-advanced-submenu');
                        } else {
                            submenu = document.getElementById(`grade-${gradeNumber}-submenu`);
                        }
                        
                        console.log('تم النقر على الصف:', gradeNumber);
                        console.log('القائمة الفرعية:', submenu);
                        
                        // إغلاق جميع القوائم الفرعية الأخرى في جميع القوائم
                        document.querySelectorAll('.grade-submenu').forEach(menu => {
                            if (menu !== submenu) {
                                menu.classList.remove('active');
                            }
                        });
                        
                        // تبديل حالة القائمة الفرعية الحالية
                        if (submenu) {
                            submenu.classList.toggle('active');
                            console.log('حالة القائمة الفرعية:', submenu.classList.contains('active'));
                        }
                    });
                });

                // إضافة وظائف لعناصر القوائم الفرعية
                const submenuItems = dropdown.querySelectorAll('.submenu-item');
                submenuItems.forEach(item => {
                    item.addEventListener('click', function(e) {
                        e.stopPropagation();
                        
                        const itemText = this.textContent.trim();
                        console.log('تم اختيار:', itemText);
                        
                        // التحقق من كتاب الطالب للصف العاشر
                        if (itemText.includes('كتاب الطالب - الصف العاشر')) {
                            const bookUrl = 'https://drive.google.com/file/d/1rx30Q2dCs3FPbJP1h8ZEsBeqpb3Jkyro/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(bookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = bookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب النشاط للصف العاشر
                        if (itemText.includes('كتاب النشاط - الصف العاشر')) {
                            const activityBookUrl = 'https://drive.google.com/file/d/17ci5vcLJ7TZ-YXmJgiBdW5-bt2VQISb0/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(activityBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = activityBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب الطالب للصف الخامس
                        if (itemText.includes('كتاب الطالب - الصف الخامس')) {
                            const grade5StudentBookUrl = 'https://drive.google.com/file/d/1N3ZHo2gKOdy4vHJ8LC7mCEomatr8Xzam/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(grade5StudentBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = grade5StudentBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب النشاط للصف الخامس
                        if (itemText.includes('كتاب النشاط - الصف الخامس')) {
                            const grade5ActivityBookUrl = 'https://drive.google.com/file/d/13I0ZXyb_RWD1-iyCCarDUNSPo9_dNcpk/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(grade5ActivityBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = grade5ActivityBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب الطالب للصف السادس
                        if (itemText.includes('كتاب الطالب - الصف السادس')) {
                            const grade6StudentBookUrl = 'https://drive.google.com/file/d/1chI8Yup3Fl4FCyoIzdqY4ElHHXpY8McP/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(grade6StudentBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = grade6StudentBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب النشاط للصف السادس
                        if (itemText.includes('كتاب النشاط - الصف السادس')) {
                            const grade6ActivityBookUrl = 'https://drive.google.com/file/d/1fEgUQYv-bYKcQh8qXRGG07NnBHsdTuMe/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(grade6ActivityBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = grade6ActivityBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب الطالب للصف السابع
                        if (itemText.includes('كتاب الطالب - الصف السابع')) {
                            const grade7StudentBookUrl = 'https://drive.google.com/file/d/1ceaiU5s7s5iAP_WE-AB4uHMJ8s8P5a2j/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(grade7StudentBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = grade7StudentBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب النشاط للصف السابع
                        if (itemText.includes('كتاب النشاط - الصف السابع')) {
                            const grade7ActivityBookUrl = 'https://drive.google.com/file/d/1WoR6Sv5bIX1qljxCpU6QNT0xM5BKOBUE/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(grade7ActivityBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = grade7ActivityBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب الطالب للصف الثامن
                        if (itemText.includes('كتاب الطالب - الصف الثامن')) {
                            const grade8StudentBookUrl = 'https://drive.google.com/file/d/1mbByx2gzk911L5oyrzlMIE14XwkSx_nL/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(grade8StudentBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = grade8StudentBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب النشاط للصف الثامن
                        if (itemText.includes('كتاب النشاط - الصف الثامن')) {
                            const grade8ActivityBookUrl = 'https://drive.google.com/file/d/1Wnzxwi1yJvAWIF1eaRhapeSwrZuSUNK5/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(grade8ActivityBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = grade8ActivityBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب الطالب للصف التاسع
                        if (itemText.includes('كتاب الطالب - الصف التاسع')) {
                            const grade9StudentBookUrl = 'https://drive.google.com/file/d/1RjU3SNCLrSwY1t-Ga5ZmyDXqdQvZSW3_/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(grade9StudentBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = grade9StudentBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        // التحقق من كتاب النشاط للصف التاسع
                        if (itemText.includes('كتاب النشاط - الصف التاسع')) {
                            const grade9ActivityBookUrl = 'https://drive.google.com/file/d/1u5mh9VWzKI9n6uu3xOpdfQPv10gnysRR/view?usp=drivesdk';
                            
                            // فتح الرابط في نافذة جديدة
                            const newWindow = window.open(grade9ActivityBookUrl, '_blank', 'noopener,noreferrer');
                            
                            if (!newWindow) {
                                // في حالة منع النوافذ المنبثقة، استخدم طريقة بديلة
                                const link = document.createElement('a');
                                link.href = grade9ActivityBookUrl;
                                link.target = '_blank';
                                link.rel = 'noopener noreferrer';
                                document.body.appendChild(link);
                                link.click();
                                document.body.removeChild(link);
                            }
                        }
                        
                        dropdown.classList.remove('active');
                        
                        // إغلاق جميع القوائم الفرعية
                        const allSubmenus = dropdown.querySelectorAll('.grade-submenu');
                        allSubmenus.forEach(menu => {
                            menu.classList.remove('active');
                        });
                    });
                });
            });

            // إضافة وظيفة فتح الفيديو للدرس الأول
            const lesson1Video = document.getElementById('lesson-1-video');

            if (lesson1Video) {
                lesson1Video.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();
                    
                    console.log('تم النقر على الدرس الأول'); // للتأكد من عمل الحدث
                    
                    // رابط Google Drive الأصلي
                    const driveUrl = 'https://drive.google.com/file/d/1ZXjR28S1r32EmEGbYuzer3hgXR3AevWk/view?usp=drivesdk';
                    
                    // فتح الرابط مباشرة في نافذة جديدة
                    const newWindow = window.open(driveUrl, '_blank', 'noopener,noreferrer');
                    
                    if (!newWindow) {
                        // في حالة منع النوافذ المنبثقة، جرب طريقة أخرى
                        const link = document.createElement('a');
                        link.href = driveUrl;
                        link.target = '_blank';
                        link.rel = 'noopener noreferrer';
                        document.body.appendChild(link);
                        link.click();
                        document.body.removeChild(link);
                    }
                    
                    // إغلاق القائمة المنسدلة
                    dropdowns.forEach(dropdown => {
                        dropdown.classList.remove('active');
                    });
                });
            } else {
                console.log('لم يتم العثور على عنصر الدرس الأول');
            }

            // إضافة وظيفة فتح الفيديو للدرس الثاني
            const lesson2Video = document.getElementById('lesson-2-video');

            if (lesson2Video) {
                lesson2Video.addEventListener('click', function(e) {
                    e.preventDefault();
                    e.stopPropagation();
                    
                    console.log('تم النقر على الدرس الثاني'); // للتأكد من عمل الحدث
                    
                    // رابط Google Drive للدرس الثاني
                    const driveUrl = 'https://drive.google.com/file/d/1I_1QqlsG3DVHsQdNtMplV_3eZq6hAYOM/view?usp=drivesdk';
                    
                    // فتح الرابط مباشرة في نافذة جديدة
                    const newWindow = window.open(driveUrl, '_blank', 'noopener,noreferrer');
                    
                    if (!newWindow) {
                        // في حالة منع النوافذ المنبثقة، جرب طريقة أخرى
                        const link = document.createElement('a');
                        link.href = driveUrl;
                        link.target = '_blank';
                        link.rel = 'noopener noreferrer';
                        document.body.appendChild(link);
                        link.click();
                        document.body.removeChild(link);
                    }
                    
                    // إغلاق القائمة المنسدلة
                    dropdowns.forEach(dropdown => {
                        dropdown.classList.remove('active');
                    });
                });
            } else {
                console.log('لم يتم العثور على عنصر الدرس الثاني');
            }

            // إضافة وظيفة إغلاق نافذة الفيديو
            const videoModal = document.getElementById('video-modal');
            const closeVideo = document.getElementById('close-video');
            const videoFrame = document.getElementById('video-frame');

            if (closeVideo) {
                closeVideo.addEventListener('click', function() {
                    videoModal.classList.remove('active');
                    videoFrame.src = ''; // إيقاف الفيديو
                    
                    // إزالة رسالة البديل إن وجدت
                    const fallbackMessage = document.querySelector('.fallback-message');
                    if (fallbackMessage) {
                        fallbackMessage.remove();
                    }
                });
            }

            // إغلاق نافذة الفيديو عند النقر خارجها
            if (videoModal) {
                videoModal.addEventListener('click', function(e) {
                    if (e.target === videoModal) {
                        videoModal.classList.remove('active');
                        videoFrame.src = ''; // إيقاف الفيديو
                        
                        // إزالة رسالة البديل إن وجدت
                        const fallbackMessage = document.querySelector('.fallback-message');
                        if (fallbackMessage) {
                            fallbackMessage.remove();
                        }
                    }
                });
            }
            
            // إغلاق القوائم عند النقر خارجها
            document.addEventListener('click', function() {
                dropdowns.forEach(dropdown => {
                    dropdown.classList.remove('active');
                });
            });
        });

        if (window.elementSdk) {
            window.elementSdk.init({
                defaultConfig,
                onConfigChange,
                mapToCapabilities,
                mapToEditPanelValues
            });
        }
    </script>
 <script>(function(){function c(){var b=a.contentDocument||a.contentWindow.document;if(b){var d=b.createElement('script');d.innerHTML="window.__CF$cv$params={r:'997565c957b3f9cd',t:'MTc2MTkzNzI1MS4wMDAwMDA='};var a=document.createElement('script');a.nonce='';a.src='/cdn-cgi/challenge-platform/scripts/jsd/main.js';document.getElementsByTagName('head')[0].appendChild(a);";b.getElementsByTagName('head')[0].appendChild(d)}}if(document.body){var a=document.createElement('iframe');a.height=1;a.width=1;a.style.position='absolute';a.style.top=0;a.style.left=0;a.style.border='none';a.style.visibility='hidden';document.body.appendChild(a);if('loading'!==document.readyState)c();else if(window.addEventListener)document.addEventListener('DOMContentLoaded',c);else{var e=document.onreadystatechange||function(){};document.onreadystatechange=function(b){e(b);'loading'!==document.readyState&&(document.onreadystatechange=e,c())}}}})();</script></body>
</html>
